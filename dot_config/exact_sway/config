set $term    alacritty
set $browser firefox
set $menu rofi -show drun

output * background ~/.local/share/backgrounds/3351632.jpg fill

include /etc/sway/config.d/*
include config.d/*
include ~/.local/share/backgrounds/bing/sway.config

exec julie-download-bing-wp
exec swayidle
exec mako

workspace 1
  # sleep to wait for foot server to start
  exec $term
  exec $term

workspace 2
  exec $browser

exec julie-download-bing-wp
