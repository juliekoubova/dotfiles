# vi:ft=gotexttmpl
{{ $is_linux := (eq .chezmoi.os "linux") }}
{{ 
  $is_wsl := (and 
    $is_linux
    (.chezmoi.kernel.osrelease | lower | contains "microsoft") 
  )
}}

{{ if ($is_wsl) }}
chrome
ff
sleepy
teams
totorohome
{{ else }}
ssh
winpwsh
{{ end }}

