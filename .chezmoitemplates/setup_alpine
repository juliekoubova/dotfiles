if [ `id -u` != "0" ]; then
  echo "Need root" >&2
  exit 1
fi

echo "Updating APK..." >&2
apk update || exit 1

{{- range .alpine }}
echo "Installing {{ . }}" >&2
apk add {{ . | quote }} || exit 1
{{- end }}

