#!/bin/sh
set -e
cd $(dirname $0)
. ./shared.sh

_target_user=`_get_target_user`

./alpine-base
./alpine-edge

apk add --virtual .julie-desktop \
  capitaine-cursors \
  chromium \
  eudev \
  firefox \
  foot \
  imv \
  less \
  libinput \
  mesa-dri-gallium \
  pm-utils \
  seatd \
  sway \
  swaybg \
  swayidle \
  swaylock \
  waybar \
  wlr-randr \
  xwayland 

./alpine-fonts 

_openrc_enable seatd

echo "Allow wheel to pm-suspend without password"
echo "permit nopass :wheel as root cmd pm-suspend" > /etc/doas.d/julie-wheel-pm-suspend.conf


if test -n "${_target_user}"; then
  adduser "${_target_user}" audio
  adduser "${_target_user}" input
  adduser "${_target_user}" seat
  adduser "${_target_user}" video
fi

echo "Setting XDG_RUNTIME_DIR"
cat << EOF > /etc/profile.d/julie-xdg-runtime-dir.sh
if test -z "\${XDG_RUNTIME_DIR}"; then
  export XDG_RUNTIME_DIR="/tmp/\$(id -u)-runtime-dir"
  if ! test -d "\${XDG_RUNTIME_DIR}"; then
    mkdir -m 0700 "\${XDG_RUNTIME_DIR}"
  fi
fi
EOF
